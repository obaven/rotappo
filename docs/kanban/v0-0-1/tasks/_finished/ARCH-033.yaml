id: ARCH-033
title: 'ARCH-3L: Primer Adapter Crate Migration'
type: Story
layer: 5
domain: core
component: adapter-primer-migration
cli_surface: []

description: |
  Move the Primer adapter into phenome-adapter-primer so
  external integration stays isolated from UI and application layers.

deliverables:
  - Primer adapter moved into `lib/adapters/phenome-adapter-primer`
  - `phenome-adapter-primer` depends on `phenome-domain` and `phenome-ports`
  - Imports updated to use `phenome_adapter_primer`

acceptance_criteria:
  - No `src/adapters/primer` module remains in the root crate
  - Adapter compiles without depending on interface or application crates
  - `cargo check -p phenome` succeeds after migration

dependencies:
  - ARCH-027

phase_gate: gitops
effort: M
risk: med

labels:
  - architecture
  - crates
  - migration
  - adapters

source_files:
  - file: src/adapters/primer
    context: "Adapter implementation to migrate"
  - file: lib/adapters/phenome-adapter-primer
    context: "Target crate for primer adapter"
  - file: src/bin
    context: "Composition roots that wire the adapter"

status: MustHaves
state: Done
status_capsule: (owner:@antigravity, updated:2026-01-04, status:done)
assignee: unassigned
