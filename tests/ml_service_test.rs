// use phenome_adapter_ml::MlService;

#[tokio::test]
async fn ml_service_constructs() {
    // TODO: Fix integration test - requires running gRPC services or mocks
    /*
    let service = MlService::new();
    let predictions = service
        .generate_recommendations("cluster-1".to_string())
        .await
        .unwrap();
    assert!(!predictions.is_empty());
    */
}
