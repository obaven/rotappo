id: ARCH-011
title: 'MIG-1C: CLI Boundary Checks (No Cross-Migration)'
type: Task
layer: 5
domain: core
component: migration-cli-boundary
cli_surface: []

description: |
  Enforce that phenome does not depend on primer CLI modules and
  bootstrappo does not import phenome CLI logic. Add automated checks
  so accidental cross-repo coupling is caught early.

deliverables:
  - Boundary check scripts (rg-based or lint tasks)
  - cargo-make task to run boundary checks
  - Documentation for allowed and forbidden imports

acceptance_criteria:
  - Checks fail if phenome references primer CLI modules
  - Checks fail if bootstrappo references phenome CLI modules
  - Boundary checks run in CI and locally

dependencies:
  - ARCH-008

phase_gate: gitops
effort: S
risk: low

labels:
  - migration
  - cli
  - guardrails
  - lint

source_files:
  - file: Makefile.toml
    context: "Guardrail task wiring"
  - file: src
    context: "Phenome codebase"
  - file: ../primer/src/cli
    context: "Bootstrappo CLI modules"

status: MustHaves
state: Done
status_capsule: (owner:@antigravity, updated:2026-01-04, status:done)
assignee: unassigned
