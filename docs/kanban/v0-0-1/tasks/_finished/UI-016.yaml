id: UI-016
title: 'Phenome TUI Action Panel + Confirmations'
type: Story
layer: 5
domain: core
component: phenome-tui-actions
cli_surface:
  - phenome ui

description: |
  Provide an action launcher in the TUI that lists available runtime
  actions, gathers parameters, and enforces safety confirmations.

deliverables:
  - Action list populated from the action registry
  - Parameter input widgets and validation
  - Confirmation modal for destructive actions
  - Action progress and result display

acceptance_criteria:
  - Action list is generated dynamically from registry metadata
  - Confirmations are required for destructive operations
  - Action result status is visible in the TUI

dependencies:
  - UI-009
  - UI-011
  - UI-014

phase_gate: gitops
effort: M
risk: med

labels:
  - ui
  - tui
  - actions
  - safety
  - phenome

source_files:
  - file: lib/phenome/src/tui/views/actions.rs
    context: "Render action list and dispatch actions"
  - file: lib/phenome/src/runtime/actions.rs
    context: "Expose action metadata and results"

status: MustHaves
state: Done
status_capsule: (owner:@antigravity, updated:2026-01-01, status:done)
assignee: unassigned
