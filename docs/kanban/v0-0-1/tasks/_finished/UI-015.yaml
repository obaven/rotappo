id: UI-015
title: 'Rotappo TUI Action + Gate Status View'
type: Story
layer: 5
domain: core
component: rotappo-tui-action
cli_surface:
  - rotappo ui

description: |
  Render action steps with readiness and gate status in the TUI. This
  view should mirror action execution order and highlight blocked steps.

deliverables:
  - Action list view with step state indicators
  - Gate details panel (deployment/daemonset/crd/statefulset readiness)
  - Filters for layer, component, and status

acceptance_criteria:
  - Step list reflects runtime snapshot ordering
  - Blocked steps show which gates are failing
  - Filtering does not require a new runtime call

dependencies:
  - UI-007
  - UI-014

phase_gate: gitops
effort: M
risk: low

labels:
  - ui
  - tui
  - action
  - gates
  - rotappo

source_files:
  - file: lib/rotappo/src/tui/views/action.rs
    context: "Render action steps and gate status"
  - file: lib/rotappo/src/runtime/state.rs
    context: "Provide action status fields for UI"

status: MustHaves
state: Done
status_capsule: (owner:@antigravity, updated:2026-01-01, status:done)
assignee: unassigned
