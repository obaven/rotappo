id: UI-021
title: 'Phenome Web Actions + Access Guardrails'
type: Task
layer: 5
domain: core
component: phenome-web-auth
cli_surface: []

description: |
  Add action triggers to the web UI and enforce access guardrails.
  Provide a local-only mode by default, with optional token-based
  access for remote use.

deliverables:
  - Action trigger UI with confirmations
  - Access guardrails (local-only default, token override)
  - Request audit logging for action executions

acceptance_criteria:
  - Destructive actions require confirmation in the web UI
  - Unauthorized requests are rejected with clear errors
  - Action audit logs include requester and timestamp

dependencies:
  - UI-009
  - UI-019

phase_gate: gitops
effort: S
risk: med

labels:
  - ui
  - web
  - actions
  - security
  - phenome

source_files:
  - file: lib/phenome/web/src/app/actions
    context: "Implement action triggers and confirmations"
  - file: lib/phenome/src/web/server.rs
    context: "Add access guardrails for action routes"

status: MustHaves
state: Backlog
status_capsule: (owner:@antigravity, updated:2026-01-01, status:backlog)
assignee: unassigned
