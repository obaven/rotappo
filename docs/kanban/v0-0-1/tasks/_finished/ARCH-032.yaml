id: ARCH-032
title: 'ARCH-3K: TUI Interface Crate Migration'
type: Story
layer: 5
domain: core
component: ui-tui-migration
cli_surface:
  - tui

description: |
  Move ratatui-based TUI logic into rotappo-ui-tui and update the
  TUI bin to depend on the new crate.

deliverables:
  - TUI interface modules moved into `crates/ui/rotappo-ui-tui`
  - `rotappo-ui-tui` depends on application, ui-core, and ui-presentation
  - Imports updated to use `rotappo_ui_tui`

acceptance_criteria:
  - No `src/interfaces/tui` module remains in the root crate
  - TUI bin compiles against the new crate
  - `cargo check --no-default-features --features tui-ratatui` succeeds

dependencies:
  - ARCH-028
  - ARCH-029
  - ARCH-030

phase_gate: gitops
effort: L
risk: med

labels:
  - architecture
  - crates
  - migration
  - tui

source_files:
  - file: src/interfaces/tui
    context: "TUI interface to migrate"
  - file: crates/ui/rotappo-ui-tui
    context: "Target crate for TUI logic"
  - file: src/bin/tui.rs
    context: "TUI composition root"

status: MustHaves
state: Done
status_capsule: (owner:@antigravity, updated:2026-01-04, status:done)
assignee: unassigned
