id: ARCH-012
title: 'MIG-1D: CLI Surface Inventory + Mapping Doc'
type: Task
layer: 5
domain: core
component: migration-cli-inventory
cli_surface:
  - terminal [actions|snapshot|action|problems|logs]
  - primer [action|reconcile|rotate|debug|nuke|cache|generate|visualize]

description: |
  Document the CLI surfaces for phenome and primer, including
  commands, flags, and output modes. This inventory serves as the
  reference for parity reviews and test updates.

deliverables:
  - CLI inventory doc with command/flag lists
  - Mapping table between phenome and primer actions
  - Update checklist for CLI changes

acceptance_criteria:
  - Inventory doc is the single source of CLI surface truth
  - Changes require updating the inventory and snapshots
  - Review checklist references the inventory

dependencies:
  - ARCH-008

phase_gate: gitops
effort: S
risk: low

labels:
  - migration
  - cli
  - documentation

source_files:
  - file: docs/architecture
    context: "CLI inventory documentation"
  - file: src/bin/terminal.rs
    context: "Phenome CLI entry"
  - file: ../primer/src/cli/commands
    context: "Primer CLI commands"

status: MustHaves
state: Done
status_capsule: (owner:@antigravity, updated:2026-01-04, status:done)
assignee: unassigned
